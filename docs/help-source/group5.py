from common import page, section, code, table, note, figure

PAGES = {
"experts": page("专家提供针对某类工作的角色与流程，专家团组织多个专家协作。先选擅长领域，再明确这一次任务的目标。", [
    section("dispatch", "选择与派遣专家",
        '''<p>从侧边栏打开<strong>专家组件</strong>，查看专家或专家团的能力介绍、擅长领域与开场白。点击<strong>派遣</strong>会准备一个带该专家的新任务；选择开场白会把内容填入输入框，仍需检查并发送。</p><p>在当前对话中，可以从输入框的<strong>添加能力或上下文</strong>菜单打开专家选择入口。派遣后，输入区显示专家名称。先确认工作区、模型和权限，再描述要完成的工作。</p><p>运行中更换专家会显示<strong>下一轮派遣</strong>，当前轮继续使用原专家。取消待派遣项只取消这次待生效的选择。已经进入会话的内容仍属于历史上下文，不会因为移除名称标签而自动清空。</p>''',
        figure("AD-01", "专家选择与派遣状态", "展示专家组件卡片中的能力介绍与派遣按钮，以及输入区的已派遣或下一轮派遣标签。"),
        code("请以代码审查专家的方式检查当前分支相对主分支的变更。优先找会影响用户的错误，给出文件位置、触发条件和验证方法，先不要修改文件。", "派遣后的任务示例")),
    section("create", "创建专家与组建专家团",
        '''<p>点击<strong>新建专家</strong>会打开新任务并预填 expert-manager 创建请求。把专家名称、擅长方向与自己的经验替换成真实内容，再提交并完成创建对话。预填草稿不会自动发送，也不表示专家已经创建成功。</p><p>希望直接填写表单时，打开新建专家右侧菜单，选择<strong>高级模式</strong>。填写名称、职业定位、Tag、能力介绍、擅长领域和开场白，并根据需要维护详细指令与能力范围。</p><p>点击<strong>组建专家团</strong>进入团队表单，选择至少两位专家并指定主理人。专家团成员必须是专家，不能把另一个专家团当成员。保存后重新查看列表，并用一个小任务验证成员分工与输出。</p>''',
        figure("AD-02", "创建专家与专家团编辑", "分别展示新建专家生成的可编辑草稿、高级模式表单、团队成员选择及主理人标记。"),
        '''<p>能力配置可继承全局能力或选择指定技能、MCP 和工具；执行时仍受实际安装状态、权限与运行环境约束。项目提供的专家只支持查看和派遣；可管理的全局专家才显示编辑与删除操作。删除组件不会替你清理既有任务中的对话记录。</p>''')
], ["web/src/components/ExpertComponentsPage.jsx", "web/src/components/ExpertCatalog.jsx", "web/src/components/ExpertEditor.jsx", "web/src/App.jsx", "web/src/components/ComposerSessionControls.jsx"]),

"swarm": page("把可以独立推进的工作交给子代理，同时保留主任务负责整合、验证和最终交付。", [
    section("parallel", "任务分工与并行执行",
        '''<p>在输入框左侧<strong>添加能力或上下文</strong>菜单选择<strong>蜂群模式</strong>，输入区出现对应标签。它会让下一条普通消息更积极地派遣子 Agent；开启状态本身不会立即启动任务，也不保证每一个请求都会产生相同数量的子任务。</p><p>适合并行的工作包括分别调查两个独立模块、检查不同平台的入口、准备测试与阅读接口。先说明每个子任务的范围、允许修改的文件和需要返回的结果，避免多个代理同时改同一处内容。</p>''',
        code("并行检查前端表单与后端校验：一位代理只阅读前端并列出边界情况，另一位只阅读后端并找出校验差异。由主任务汇总证据后提出修复方案，暂不修改文件。", "可独立并行的任务示例"),
        '''<p>子代理拥有独立上下文，并继承父任务的工作目录与权限模式。主任务通常收到子代理最终答复，而非自动把所有中间工具输出复制进自身上下文。共享工作目录意味着文件变化会相互可见；独立上下文不等于独立 Git 工作树。</p>''',
        figure("AD-03", "开启蜂群模式并提交分工", "展示添加能力菜单中的蜂群模式、开启后的标签，以及说明任务边界的输入内容。")),
    section("progress", "查看进度与处理操作确认",
        '''<p>产生子任务后，在主会话的<strong>后台任务</strong>面板查看运行中和已完成的任务，打开任务可阅读其会话与工具结果。子任务归属于父会话，不会作为普通任务充满主侧边栏和全局搜索结果。</p><p>来自子任务的权限请求与问题会在主界面出现，并标注来源。检查任务名称、工具参数与目标路径后处理；不要仅因为主任务尚有输出，就忽略另一个子任务正在等待回答。</p><p>可以在面板中停止运行中的子任务。停止父任务也会向其子任务传播中断；已经产生的文件和记录会保留，不自动回滚。清除已结束任务会删除这些子会话记录，确认不再需要查看后再清理。</p>''',
        code("/tasks\n/tasks list\n/tasks abort 子任务ID\n/tasks clear", "TUI · 管理后台任务"),
        figure("AD-04", "后台任务与来自子任务的请求", "展示运行中与已完成分组、查看会话和停止入口，以及带来源名称的权限确认。"),
        '''<p>全部子任务完成后，让主任务检查它们之间的冲突、统一差异并运行必要验证。各自报告“完成”不能代替整合后的构建或测试。</p>''')
], ["web/src/components/InputBar.jsx", "web/src/components/ComposerSessionControls.jsx", "web/src/components/SubagentPanel.jsx", "src/commands/builtin_commands.cpp", "docs/subagents.md"]),

"schedules": page("把清晰且可重复的任务保存为循环，按周期、间隔或指定时间执行，并通过记录检查每次结果。", [
    section("create", "创建与设置执行时间",
        '''<ol><li>打开侧边栏<strong>定时任务</strong>，进入“循环”页面，点击<strong>添加</strong>或选择模板。</li><li>填写名称与提示词，选择工作空间；与项目无关的任务可不选择工作空间。</li><li>选择模型与权限。需要 Git 隔离时，在选定工作空间后启用工作树。</li><li>选择执行频率，按需设置生效和结束时间。</li><li>保存后核对下一次执行时间和启用状态。</li></ol>''',
        table(["频率", "设置方式", "适合的例子"], [["周期", "每天、工作日或每周指定日期与时间。", "工作日早上检查仓库待处理事项。"], ["按间隔", "每隔指定分钟、小时或天执行。", "定期检查本地生成结果。"], ["单次", "指定一个日期与时间。", "稍后执行一次已准备好的检查。"]]),
        note("保存前检查权限", "循环表单提供默认权限与完全访问权限。当前新建默认可能为完全访问权限，请主动核对。默认权限下需要确认的动作会进入等待用户状态；只有明确允许无人值守执行的操作才适合自动放行。"),
        '''<p>提示词应说明数据来源、允许的修改和完成条件，避免“继续上次工作”这类依赖未提供上下文的描述。当前表单中的连接器选择尚不可用，不要依赖它发送结果。</p>''',
        figure("AD-05", "循环创建与执行时间", "展示名称、工作空间、工作树、模型、权限和频率设置，使用一个无敏感数据的只读检查任务。"),
        code("检查此工作区的 Git 状态和最近提交，生成一份简短摘要。只读取本地文件，不修改文件、不提交、不推送。遇到错误时记录原因并结束。", "循环提示词示例")),
    section("manage", "查看记录、暂停与管理",
        '''<p>列表展示下一次执行与最近运行情况。打开运行记录查看等待执行、执行中、等待用户、已完成、失败或已错过等状态，并检查关联任务的输出。失败应结合原因处理，不能只看循环仍为启用状态。</p><p><strong>停用</strong>用于暂停后续计划，重新<strong>启用</strong>后检查新的执行时间；需要停止正在运行的那一轮，应打开对应任务并停止。编辑时可调整提示、模型与时间。删除循环也会移除其运行记录，保留需要的信息后再删除。</p><p>daemon 必须在执行时间保持运行，模型和工作目录也要可用。计算机离线、休眠或后台停止期间错过的执行会记录为已错过，不应期待恢复后把每次历史执行逐一补跑。同一工作空间的时间冲突会在保存或启用时提示，运行时占用也可能导致错过。</p><p>远端后台按其时间配置调度。跨时区或系统时区变化后，重新核对下一次执行时间；不要只按浏览器所在设备的时钟推测。</p>''',
        figure("AD-06", "循环列表与运行记录", "展示下一次执行、启用或停用按钮和一条真实运行记录，标出状态、原因及查看任务入口。"))
], ["web/src/components/LoopPage.jsx", "web/src/lib/loops.js", "src/loop/loop_types.hpp", "src/loop/loop_scheduler.cpp", "src/loop/loop_store.cpp"]),

"agent-browser": page("在桌面端把需要处理的网页共享给智能体，让浏览、定位和操作发生在你能看到的页面中。", [
    section("open", "打开浏览器面板",
        '''<p>从桌面右侧<strong>打开工具 &gt; 浏览器</strong>新建浏览器页签，输入地址后导航。没有协议的主机名会按 HTTPS 处理；普通搜索词可发起搜索。当前原生 Agent 浏览器支持 Windows 和 macOS 14 及以上的相应桌面构建，Linux 与直接 Web 页面不具备同样的原生桥接能力。</p><p>这里的页面属于 ACECode 内置浏览器。它使用自己的持久化浏览器配置，页面间可共享该配置中的登录信息；不会直接继承你日常 Edge 或 Safari 的全部登录状态。</p>''',
        figure("AD-07", "浏览器页签与地址栏", "展示打开工具菜单、浏览器页签、地址输入及加载状态，使用公开示例页面。")),
    section("share", "决定哪些页面交给智能体",
        '''<p>你手动创建的页面默认不共享。需要让智能体读取或操作时，在目标页签开启<strong>与智能体共享</strong>。智能体通过浏览器工具创建的页面默认共享；每个页面的状态独立。</p><p>未共享的页面不会因其正在前台就自动开放给工具。开始任务前明确目标页签与操作范围，例如“检查这个页面的表单校验，提交前先让我确认”。登录过程和敏感表单也应在正确页面中处理。</p>''',
        figure("AD-08", "页面共享与可见操作", "展示单个页签的共享开关、共享状态，以及智能体操作时的边框或光标反馈，遮挡任何账号信息。"),
        '''<p>隐藏面板会保留页面状态；关闭页签会释放该页面。遇到操作失败时先检查它是否仍存在、是否共享和是否加载完成，再让智能体继续。</p>'''),
    section("context", "引用页面元素与控制台信息",
        '''<p>元素选择可以把目标位置作为上下文加入输入区，按 <kbd>Esc</kbd> 退出选择。添加控制台信息时，引用的是当时的快照，不会自动发送消息，也不会持续同步未来所有日志。</p><p>提交前检查附件或上下文项是否指向正确页面，可以移除无关项。动态页面、跨来源 iframe 和不同操作系统的原生实现存在能力差异；选不到时描述页面和元素位置，并以实际返回信息为准。</p><p>让智能体完成操作后，检查网页结果与必要的应用数据。浏览器动作成功只证明交互执行，不代表业务提交一定成功。连接能力状态可以在<strong>设置 &gt; 工具</strong>检查。</p>''')
], ["docs/agent-browser.md", "web/src/components/PreviewDetailsPanel.jsx", "web/src/components/SettingsPage.jsx"]),

"channels": page("通过已经安装的渠道插件把一个 ACECode 会话连接到外部消息平台，或使用手动 Webhook 进行集成。", [
    section("configure", "准备并连接消息渠道",
        '''<p>先按渠道插件的说明安装它的可执行程序、完成平台认证并取得 manifest 文件。ACECode 使用 <code>remote_control.channels</code> 保存插件配置，使用 <code>remote_control.default_channel</code> 指定默认渠道。只输入消息平台名称不会自动安装插件。</p>''',
        code('{\n  "remote_control": {\n    "default_channel": "my-channel",\n    "channels": {\n      "my-channel": {\n        "manifest_path": "/absolute/path/to/channel-plugin.json",\n        "timeout_ms": 10000,\n        "settings": {}\n      }\n    }\n  }\n}', "JSON · 合并到主配置，路径与 settings 按插件说明填写"),
        '''<p>在要绑定的会话中提交 <code>/rc</code>，它是 <code>/remote-control</code> 的别名，会激活默认渠道。确认返回已连接状态后，从消息平台发一条简单请求，检查是否进入目标会话并能收到回复。</p>''',
        figure("AD-09", "渠道绑定与远程消息", "展示 /rc 返回的已连接状态和会话绑定标记，以及一条测试消息的往返；必须遮挡 Token、Webhook 密钥与私人聊天。")),
    section("sessions", "在渠道中切换会话",
        '''<p>daemon 托管的已连接渠道可使用以下会话导航命令。这些控制命令由后台处理，不会作为普通问题交给当前模型。</p>''',
        table(["在渠道发送", "结果"], [["<code>/sessions</code>", "列出最近十个普通、未归档会话。"], ["<code>/sessions more</code> 或 <code>/sessions all</code>", "查看完整目录。"], ["<code>/sessions search 关键词</code>", "搜索最多五个匹配结果。"], ["<code>/sessions 2</code>", "选择最近一次列表中的第 2 个会话。"]]),
        '''<p><code>/session</code> 和 <code>/resume</code> 是同一组渠道导航的别名。列表包含有工作区与无工作区的普通任务，不包含已归档任务和子代理。切换成功后以确认信息中的会话名称为准，再发送后续工作。</p><p>目标会话无法恢复时，原连接会保留。运行中的控制请求还在处理时，不要连续发送大量列表和切换命令。桌面前端可以收到切换提示，但渠道本身的会话选择不依赖桌面窗口保持打开。</p>'''),
    section("manage", "查看状态、解绑与手动接入",
        code("/rc show\n/rc off", "在 ACECode 会话中查看状态与解除绑定"),
        '''<p>show 展示当前连接、入站与出站状态，内容可能包含认证信息，不要直接公开截图。off 解除当前绑定。daemon 托管模式会保存绑定会话并在后续启动时尝试恢复，单纯关闭桌面窗口不等于主动解绑。</p><p>手动集成使用 <code>/remote-control on</code> 启动本地入站服务，再通过 <code>/remote-control url &lt;webhook-url&gt;</code> 指定出站地址。入站使用 <code>POST /rc/send</code>、<code>X-ACECode-RC-Token</code> 请求头和 <code>{"text":"消息"}</code> 正文；按 show 的实际地址接入。</p><p>远程消息控制与<a href="web.html#remote">远程 Web</a>是两套入口，认证头与端口也不同。需要实现自己的渠道时，参见<a href="extension-development.html#channel">渠道插件协议</a>。</p>''')
], ["src/config/config.hpp", "src/commands/remote_control_command.cpp", "src/remote_control/session_channel_binder.cpp", "docs/channel-plugin-protocol.md"]),

"examples": page("用四个完整流程练习把目标、范围、执行和验证连在一起。示例提示可直接修改后使用。", [
    section("new-project", "新项目实战",
        '''<ol><li>从新任务的工作区选择入口使用<strong>新建项目</strong>，填写名称并检查默认位置与实际目录名。</li><li>创建后确认该目录已成为当前任务工作区，选择模型与权限。</li><li>先描述目标用户、功能范围、技术约束和如何验收。</li><li>让 ACECode 检查空目录、给出简短实现步骤，再逐步创建文件。</li><li>运行项目的构建或启动命令，检查实际页面或输出。</li></ol><p>新建项目创建的是空目录，不会自动选定技术栈或生成完整应用。名称不符合系统规则时，界面会展示规范化后的目录名；保存前检查路径预览。</p>''',
        code("在当前空项目中创建一个本地待办网页，使用普通 HTML、CSS、JavaScript，不依赖服务端。需要新增、完成和删除待办，并在刷新后保留数据。先说明文件结构，完成后验证这四个操作，并告诉我如何打开。", "发送前按实际需求调整"),
        figure("AD-10", "新建项目与首次运行结果", "展示项目名称、默认位置、创建后的工作区，以及第一次运行的检查位置；结果页面留待真实实现后截图。")),
    section("debug", "排查问题与修复缺陷",
        '''<p>先提供复现步骤、预期结果、实际错误与相关日志。通过 @ 或文件预览引用入口文件，要求先追踪真实调用链。原因明确后再给出修改范围，修复后重复同一复现步骤。</p>''',
        code("登录成功后第一次打开设置页会出现空白，刷新后恢复。请先检查路由和初始化请求，结合我附上的错误日志定位原因，不要先改代码。确认原因后修复最小范围，并用原复现步骤验证。", "分阶段诊断与修复"),
        '''<p>如果任务在模型连接前就失败，先按<a href="troubleshoot-models.html">模型连接排错</a>恢复请求能力。排查应用代码与排查 ACECode 自身连接是不同层面，不要混用错误证据。</p>'''),
    section("feature", "实现功能与重构代码",
        '''<p>先说明用户触发动作及修改后的行为，列出兼容要求。需要隔离实验时，在新任务首次发送前启用 Git 工作树并选择基准分支。让 ACECode 阅读现有实现与测试，然后完成修改和对应验证。</p>''',
        code("给现有任务列表增加按名称筛选。输入时更新列表，清空后恢复全部任务，保留当前排序和置顶逻辑。先阅读相关组件与测试，复用现有样式，完成后验证中文、空白输入和无匹配结果。", "功能请求示例"),
        '''<p>重构时明确希望保持哪些公开行为、接口和数据格式。不要以文件变少或函数变短作为唯一验收条件。完成后在差异面板检查范围，并确认已有行为仍通过验证。</p>'''),
    section("review", "审查代码变更",
        '''<p>打开变更面板，选择要比较的基准并检查涉及的文件。告诉 ACECode 审查对象、关注的风险和是否允许直接修改；如果仅需审查，在请求中明确写出。</p>''',
        code("审查当前分支相对 main 的差异，先不要修改。优先检查可能造成数据丢失、错误状态和兼容性回归的问题。每项发现注明文件位置、触发条件、实际影响和建议验证方式；没有证据的猜测不要列为缺陷。", "main 应替换为仓库真实基准分支"),
        '''<p>审查结果应能对应具体代码与行为。确认修复建议后再启动修改任务，并在结束时重新查看差异。变更面板可能包含你或其他任务产生的修改，判断范围时不要把所有差异都归因于这一次任务。</p>''')
], ["web/src/components/CreateProjectModal.jsx", "web/src/components/GitChangesPanel.jsx", "web/src/components/InputBar.jsx", "web/src/lib/gitSessionPill.js", "docs/user-manual.md"]),
}
