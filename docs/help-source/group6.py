from common import page, section, code, table, note, figure

PAGES = {
"faq": page("从最常见的问题找到对应操作，进一步的诊断步骤可进入相应排错章节。", [
    section("start", "安装完成后为什么还不能开始对话",
        '''<p>先在设置的模型页添加有效预设，再在任务中选中它。安装 ACECode 不会自动为所有服务商创建账号、购买额度或授予模型权限。填写的 Model ID、接口协议、地址与密钥都必须属于同一个可用服务。</p><p>如果已经保存模型却连接失败，先查看错误文字和状态码，再按<a href="troubleshoot-models.html">模型连接与认证问题</a>处理。反复新建任务通常不能修复同一份错误连接配置。</p>'''),
    section("files", "为什么找不到文件或修改到了不同目录",
        '''<p>检查当前任务的工作区及是否使用 Git 工作树。工作树有自己的磁盘路径；远程 Web 访问的是后台计算机中的路径；无工作区任务不应被当成某个已有项目。</p><p>路径引用表示让智能体按需读取，附件表示已上传的快照。两者的内容来源和更新方式不同。修改前让 ACECode 确认目标路径，修改后在相同目录查看差异。</p>'''),
    section("waiting", "为什么任务一直等待或不能直接继续",
        '''<p>先检查是否等待工具确认、问题回答、排队消息、后台子任务或网络重试。处理界面中带来源的请求，或根据需要停止任务；不要在不清楚状态时连续重复发送相同消息。</p><p>完全访问权限主要影响工具确认，不会取消所有业务提问。需要无人值守流程时，要同时准备明确任务和适当的问题处理策略。</p>'''),
    section("history", "停止、归档、分叉和删除有什么区别",
        table(["动作", "主要影响"], [["停止", "停止继续执行；已完成的文件修改保留。"], ["归档", "把任务移入归档状态，后续可恢复；不删除项目文件。"], ["分叉", "从选定上下文形成另一条会话路径；不自动恢复代码。"], ["删除或清除记录", "可能永久移除相应会话数据，应以操作确认范围为准。"]]),
        '''<p>恢复文件请阅读<a href="git.html#restore">回退与恢复</a>。导出会话得到的 Markdown 用于阅读和分享，不能当作完整的会话数据库备份。</p>'''),
    section("surfaces", "为什么 Web 中没有某个桌面入口",
        '''<p>原生文件选择、系统通知、Agent 浏览器等能力依赖桌面桥接与平台。浏览器中的普通 Web 仍可使用模型、任务和许多管理功能，但不会获得所有桌面原生权限。</p><p>选择界面可参考<a href="index.html#surfaces">桌面端、终端与 Web 如何取舍</a>。本帮助包本身可离线打开；实际模型和在线工具是否可用，取决于各自的连接环境。</p>''')
], ["docs/user-manual.md", "docs/daemon-api.md", "web/src/components/SettingsPage.jsx", "web/src/lib/inputBarState.js"]),

"troubleshoot-install": page("先确认安装来源、实际启动文件和运行环境，再处理依赖、端口与更新问题。", [
    section("command", "找不到 acecode 命令",
        '''<ol><li>重新打开终端，让新 PATH 生效。</li><li>检查安装目录中是否存在对应程序，区分终端版与桌面程序。</li><li>检查命令解析到了哪一个文件，排除旧版或其他安装路径。</li><li>直接运行目标程序确认版本，再修复 PATH。</li></ol>''',
        code("Get-Command acecode -All\nacecode --version", "Windows PowerShell"),
        code("command -v acecode\nacecode --version", "macOS / Linux"),
        '''<p>如果使用 npm 安装，检查 Node.js、npm 与全局可执行目录是否属于同一环境。公司电脑可能有多套 Node 版本或终端环境，旧终端不一定继承新的安装路径。重新安装前先按<a href="installation.html">安装</a>确认平台与安装方式。</p>'''),
    section("launch", "启动失败或窗口立即退出",
        '''<p>记录系统版本、CPU 架构、安装包名称和错误提示。Windows 桌面需要可用的 WebView2 环境；macOS 需要与芯片匹配的应用；Linux 二进制还受到系统运行库与执行权限影响。</p><p>独立后台启动失败时，先运行 status；确认没有正在使用的同一后台后，使用前台模式查看错误。桌面托管后台有自己的运行目录，不能仅凭普通 daemon status 判断桌面进程是否存在。</p>''',
        code("acecode daemon status\nacecode daemon --foreground", "独立后台 · 前台命令会占用当前终端"),
        '''<p>端口占用会导致独立 daemon 明确拒绝启动，不会自动扫描备用端口。先识别占用程序及使用者，再决定关闭该程序或为独立后台明确指定其他端口。</p>'''),
    section("update", "更新失败或仍显示旧版",
        '''<p>检查更新源是否可访问、磁盘空间是否足够，以及目标程序是否被正在运行的进程锁定。保留错误输出，确认启动路径后按原安装方式重试更新，避免同时维护多个互相覆盖的副本。</p><p>更新命令成功后，重新打开目标应用并核对版本。桌面启动器与配套 daemon 应来自同一套安装包；不要只替换其中一个程序来测试正式安装。</p><p>配置错误触发自动回滚时，先查看回滚提示与备份位置，修复目标字段。卸载通常不等于清除所有个人数据；按<a href="update-uninstall.html">更新与卸载</a>选择是否保留模型和历史。</p>''',
        figure("TS-01", "安装或更新错误的有效截图", "截取完整错误文字、目标安装包或版本信息和发生时间；避免只截一个没有上下文的失败图标。"))
], ["src/daemon/cli.cpp", "docs/daemon-api.md", "docs/user-manual.md", "src/desktop/strings.cpp"]),

"troubleshoot-models": page("按服务商、预设、实际端点和错误类别逐项检查，避免把所有失败都归因于模型本身。", [
    section("fields", "先核对连接的四项信息",
        '''<p>进入模型页编辑目标预设，确认 Provider 类型、Model ID、Base URL 或完整端点及认证来源。预设名称只是 ACECode 中的标识，可以与 Model ID 不同。复制名称相同的预设时，尤其要检查实际地址是否一致。</p><p>保存后确认当前任务选中的是这份预设。更新默认模型主要影响后续选择；正在运行的请求不会因为刚刚编辑字段而瞬间改用新参数。</p>''',
        figure("TS-02", "模型连接排错信息", "展示预设名称、Provider、Model ID、去除敏感参数后的地址，以及错误状态码；API Key 必须完整遮挡。")),
    section("errors", "按错误类别处理",
        table(["现象", "优先检查"], [["401 / 认证失败", "密钥是否有效、是否已过期、请求是否发送到正确服务；托管账号是否仍授权。"], ["403 / 无权限", "账号、项目或组织是否有目标模型权限，是否受服务策略限制。"], ["404 / 模型不存在", "Model ID、Base URL 与端点路径；区分找不到路由和找不到模型。"], ["400 / 参数不支持", "协议是否匹配，思考、采样、工具与输出参数是否被该模型接受。"], ["429", "区分临时频率限制与余额、套餐或硬配额不足，阅读服务返回的具体说明。"], ["Failed to fetch / 连接失败", "先确认失败请求指向本机 daemon 还是模型服务，再检查服务、代理和网络。"]]),
        '''<p>服务端错误文字有时比状态码更具体。反馈时保留错误类别和时间，删去密钥、认证头与私密请求内容。不要通过关闭证书验证来掩盖错误的地址或系统证书环境。</p>'''),
    section("probe", "探测与账号授权",
        '''<p>探测失败先检查服务是否支持模型目录接口；没有返回完整能力信息，不一定表示对话接口无法使用。查看已有探测结果可能来自缓存，需要重新请求服务时点击<strong>重新探测</strong>。</p><p>GitHub Copilot 使用模型页的连接 GitHub 流程完成设备授权，确认浏览器授权后回到 ACECode 检查连接状态。退出连接后需要重新授权，不能用普通自定义接口的密钥表单代替托管流程。</p><p>先用一条短文本验证基础请求，再测试图片、思考或工具调用。某项能力失败时按<a href="model-capabilities.html">模型能力与参数</a>核对，而不是同时修改所有配置。</p>''')
], ["web/src/components/model-settings/ModelSettingsSection.jsx", "web/src/components/model-settings/ModelProfileDialog.jsx", "web/src/components/model-settings/ModelProbeDialog.jsx", "docs/daemon-api.md"]),

"troubleshoot-context": page("上下文超限、短暂网络故障和任务逻辑失败需要分别处理。先读取当前状态，再决定压缩、等待或修复配置。", [
    section("overflow", "上下文超限",
        '''<p>检查所用模型真实支持的上下文窗口与最大输出限制。模型请求还包含项目规则、工具定义、历史消息、图片等内容，不能只按当前输入框字数估算总长度。</p><ol><li>确认预设中的窗口与输出配置符合服务商限制。</li><li>减少大日志、重复附件和无关历史。</li><li>有可用模型连接时提交 /compact，再检查压缩是否完成。</li><li>仍无法继续时，新建任务并附上目标、已确认结论、未完成事项和必要路径。</li></ol><p>单纯增大本地窗口数值不会扩展服务端限制。修改模型配置后，确认后续请求确实使用了更新后的预设。</p>'''),
    section("retry", "网络暂时不可用，等待重试",
        '''<p>临时网络故障、超时或可恢复的服务端错误会进入自动重试。界面会显示重试状态和等待信息；间隔逐步增加，最长可能达到 20 分钟，次数本身没有固定上限。</p><p>在同一进程持续运行期间可以等待恢复，也可以使用停止操作取消。重试针对失败的模型采样，不会为了这次重试重新执行已经成功的工具。停止或进程退出后，不应期待这段等待自动在新进程中继续。</p><p>认证错误、确定的无权限、硬额度不足、非法参数和上下文超限不适合靠无限等待解决。先检查具体错误，修复后再发送一条小请求验证。</p>''',
        figure("TS-03", "自动重试与停止入口", "展示网络重试状态、可见等待信息和当前任务停止入口，用图注明确等待不表示新消息已经发送。")),
    section("partial", "有部分输出但没有完成",
        '''<p>部分文字或部分工具成功不表示整项任务完成。检查最后状态是完成、失败还是中断，查看最后一个错误，并核对文件是否已经变化。继续时说明从哪一步接着做，避免重复已经完成的外部操作。</p><p>压缩失败时保留错误记录，不把进度提示当成成功标记。无界面 CLI 应同时检查退出码与 JSON 中的 is_error；API 客户端应检查终态 outcome，而不只依据收到 done 事件。</p><p>需要提交问题时，记录模型预设、最近成功步骤、失败时间和上下文状态，参见<a href="feedback.html">日志与问题反馈</a>。</p>''')
], ["docs/user-manual.md", "docs/daemon-api.md", "src/headless/headless_runner.cpp"]),

"troubleshoot-tools": page("先判断是能力没有加载、运行条件不满足，还是调用后的业务结果失败，再处理对应环节。", [
    section("tools", "工具不可用或执行失败",
        '''<p>用 TUI 的 /tools 或 CLI 的 <code>acecode -p --list-tools</code> 检查当前工具清单。平台、功能开关、专家能力范围及运行依赖都可能影响可用性。Web 设置工具页不是完整的工具注册清单。</p><p>权限被拒时检查请求范围与当前模式；命令找不到时检查后台所在环境的 PATH；文件找不到时检查真实工作区与工作树路径。文件精确编辑失败，应重新读取目标内容，不能不断重试同一个已失效片段。</p><p>工具正常执行但测试失败时，阅读测试错误，继续修复代码。语言服务器无反馈时先按<a href="lsp.html">LSP 与代码诊断</a>确认其安装和项目环境。</p>'''),
    section("skills", "技能没有出现或没有应用",
        '''<ol><li>确认是一个完整目录，存在有效的 SKILL.md 和必需的名称、描述。</li><li>检查目录位于当前启用的全局、项目或额外扫描位置。</li><li>重新加载技能，并检查禁用状态、同名来源与解析诊断。</li><li>在一个小任务里明确选择技能，确认辅助文件的相对路径。</li></ol><p>复制技能正文到聊天不等于安装技能；安装成功也不等于它需要的脚本依赖已准备好。CLI 默认不启用技能，需要显式选择。具体步骤见<a href="skills.html">技能</a>。</p>'''),
    section("mcp", "MCP 连接或工具加载失败",
        '''<p>先确认 JSON 合法且已经保存，再查看服务器状态和工具清单。stdio 服务检查 command、args、环境变量和解释器；远程服务检查 sse / http 协议、URL 拼接、认证与代理。</p><p>修改服务器配置后使用 Reload 或 <code>/mcp reconnect 服务器名</code> 重连；重新加载编辑区文本与重建连接不是同一个操作。显示“重启 daemon 后生效”时按提示完成重启。</p><p>连接成功但没有工具可能是服务没有提供 tools，或初始化、能力协商失败。模型调用时失败则进一步查看工具参数和服务返回内容。先用一个明确的简单工具验证，不要一次同时新增多个服务器后猜测是哪一个导致问题。</p>'''),
    section("hooks", "Hook 没有执行",
        '''<p>在钩子页刷新并查看来源、信任状态、事件和匹配规则。待审核项、被禁用项、未受信任项目或不支持的处理器会被跳过。脚本的解释器、工作目录和超时同样需要正确。</p><p>修改命令后可能需要重新信任。连接器的首次启动认证另有规则，不会因为重启或切换连接器开关就自动再次执行。参见 <a href="hooks.html">Hooks</a> 与<a href="connectors.html">连接器</a>。</p>''')
], ["docs/skills.md", "docs/hooks.md", "src/commands/builtin_commands.cpp", "web/src/components/SettingsPage.jsx", "src/tool/builtin_tool_registry.hpp"]),

"troubleshoot-desktop": page("把桌面壳、后台服务、浏览器连接和项目工具分开检查，先保留现场再采取重启等操作。", [
    section("desktop", "桌面白屏、连接失败或意外退出",
        '''<p>记录发生时间、应用版本、最后一次操作和完整错误。先查看最近 desktop 与 daemon 日志，确认是窗口、托管后台还是模型请求出了问题。只看到 Failed to fetch 不能直接判断模型服务故障。</p><p>桌面使用专门的托管运行目录 <code>~/.acecode/run/desktop-shared/</code>，启动时检查进程身份、心跳和协议兼容性。普通 <code>acecode daemon status</code> 查看的是独立后台目录，结果不能直接替代桌面托管状态。</p><p>需要重新启动前，先保存尚未保存的文件草稿并记录正在运行的任务。macOS 关闭窗口通常只是隐藏应用；真正退出后是否继续运行后台由全局偏好决定，应区分这两种动作。</p>''',
        figure("TS-04", "桌面故障的定位信息", "展示不含密钥的完整错误、应用版本，以及对应时间范围的日志摘录；分别标注桌面壳和后台错误。")),
    section("terminal", "终端不可用或命令环境不同",
        '''<p>在底部终端选择正确的 Shell，并检查命令实际运行目录。桌面启动方式可能与外部终端继承不同的 PATH、代理或虚拟环境，外部命令可用不代表后台环境中也可用。</p><p>原生 PTY 与兼容终端模式的能力不同。兼容模式不适合 vim、top 等需要完整交互终端的程序；需要这些工具时使用可用的原生终端或外部终端。</p><p>TUI 按键不符合预期时，检查输入焦点、下拉菜单和权限弹层。Ctrl+P 等按键会因当前模式而改变作用；退出使用 /exit，或按提示连续两次 Ctrl+C。</p>'''),
    section("remote", "远程 Web 无法连接",
        '''<ol><li>先在服务所在机器打开实际本机 Web 地址。</li><li>检查远程 Web 模式已开启，并使用复制连接中的代理地址与端口。</li><li>确认目标设备能解析主机名并到达端口，检查防火墙或网络策略。</li><li>从当前运行服务重新复制连接，排除旧 Token。</li><li>观察是 HTTP 请求失败还是 WebSocket 断开，再查看后台与代理日志。</li></ol><p>daemon 固定监听回环地址，远程接入通过独立代理提供。不要把出站模型代理配置当作远程 Web 配置。服务重启、代理切换或端口配置变化后，先刷新实际连接信息。</p>'''),
    section("browser", "Agent 浏览器或预览功能不可用",
        '''<p>确认使用的是支持原生桥接的平台桌面构建。对于浏览器页面，检查该页签是否共享、是否已加载、是否已关闭。外部浏览器标签页不会自动变成 ACECode 的可控页签。</p><p>文本文件在工作区外只读是预览边界；保存冲突说明磁盘内容可能已改变。先比较当前草稿和磁盘文件再处理，避免覆盖其他程序的新修改。参考<a href="desktop.html">桌面端</a>和<a href="agent-browser.html">Agent 浏览器</a>。</p>''')
], ["docs/daemon-api.md", "src/desktop/main.cpp", "web/src/components/ConsoleDock.jsx", "docs/agent-browser.md"]),

"feedback": page("提供能复现的问题描述和对应时间范围的日志，帮助快速定位到具体会话与运行环节。", [
    section("report", "先整理问题描述",
        code("ACECode 版本：\n操作系统与架构：\n使用入口：桌面 / TUI / CLI / Web\n发生时间与时区：\n复现步骤：\n预期结果：\n实际结果与错误文字：\n是否每次发生：\n已经尝试的处理：", "问题描述模板"),
        '''<p>给出最短的复现步骤，以及第一次失败前最后一个成功动作。涉及模型时提供 Provider 类型、Model ID 和脱敏后的错误；涉及文件时提供必要的路径结构与差异，避免提交整个私有项目。</p>'''),
    section("submit", "通过问题反馈页面提交",
        '''<p>进入<strong>设置 &gt; 问题反馈</strong>，填写反馈内容。页面默认附带最近的 desktop 与 daemon 日志；需要关联一次具体任务时，在会话选择中选中它。</p><p>没有选择会话时，不附带会话数据库或会话记录。选中后会显示将附带的会话，可以清除选择。点击<strong>提交反馈</strong>会上传反馈包，成功后显示包文件名；上传失败时查看错误及可能返回的本地包路径。</p><p>选择会话意味着把其内容加入反馈范围。提交前确认其中没有不应共享的业务资料、凭据或私人内容。页面没有提供的本地导出操作不要当成上传按钮的替代行为。</p>''',
        figure("TS-05", "反馈内容与可选会话附件", "展示反馈文本、会话选择、未选择时的附件说明和提交结果；使用虚构示例内容，遮挡真实用户数据。")),
    section("logs", "手动查找日志",
        table(["运行方式", "常见日志位置"], [["TUI", "当前工作目录中的 <code>acecode.log</code>。"], ["独立或桌面后台", "<code>数据目录/logs/daemon-日期.log</code>。"], ["桌面壳", "<code>数据目录/logs/desktop-日期.log</code>。"], ["无界面 CLI", "<code>数据目录/logs/headless-日期.log</code>。"]]),
        '''<p>个人模式的数据目录为 <code>~/.acecode</code>，Windows 服务使用 <code>%PROGRAMDATA%/acecode</code>。尽量截取事件前后连续一小段日志，保留时间、错误类别和相关 ID，遮挡认证头、Token、密钥及不必要的个人信息。</p><p>也可以在项目官方问题页提交经过整理的描述。不要直接上传主配置文件、run/token 或整个用户数据目录；需要补充材料时按实际诊断范围提供。</p>''')
], ["web/src/components/SettingsPage.jsx", "src/feedback/feedback_upload.cpp", "src/desktop/main.cpp", "src/headless/headless_runner.cpp", "src/daemon/worker.cpp", "src/main.cpp"]),
}

from group6_reference import PAGES as REFERENCE
PAGES.update(REFERENCE)
