## 1. 模型选择回填逻辑

- [x] 1.1 增加探测模型批量替换草稿的纯辅助函数，保留名称和连接字段并记录每个模型的探测元数据。
- [x] 1.2 增加 JavaScript 单元测试，覆盖多选、单选、空选择、名称保留和上下文窗口元数据。

## 2. 探测模型选择弹窗

- [x] 2.1 新增聚焦的 `ModelProbeDialog`，实现搜索、加载/错误/空状态、多选或单选、已选计数和严格关闭边界。
- [x] 2.2 在自定义 OpenAI 兼容 `Model ID` 标题行接入“探测模型”按钮，复用现有探测请求并在有效确认后事务性回填草稿。

## 3. 回归验证

- [x] 3.1 更新模型设置结构回归测试，固定按钮位置、嵌套弹窗层级、遮罩/Escape 禁止关闭、叉号取消和有效确认关闭行为。
- [x] 3.2 运行相关 Web 单元/结构测试、`pnpm test`、`pnpm build`、严格 OpenSpec 校验与 `git diff --check`，记录任何无关既有失败。

## 4. 本地探测缓存

- [x] 4.1 为 `state.json` 增加模型探测缓存 typed helper，以不透明连接指纹存储模型、上下文和探测时间，并覆盖容错、覆盖写和共存测试。
- [x] 4.2 扩展模型探测请求的显式 Provider catalog ID，增加稳定连接指纹、成功写缓存和只读缓存端点；验证缓存读取不访问上游且状态不泄漏连接密钥。
- [x] 4.3 让 Provider 选择器在连接变化时只读恢复缓存，缓存命中时不自动重新探测，并提供明确的首次探测/查看结果/重新探测语义。
- [x] 4.4 增加前端 API 与结构回归测试，运行聚焦 C++/Web 测试、生产构建、严格 OpenSpec 校验和 `git diff --check`。
