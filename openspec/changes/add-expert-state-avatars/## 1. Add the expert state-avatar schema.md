## 1. Add the expert state-avatar schema

- [ ] 1.1 Add shared fixed-state definitions for `working`, `needs_attention`, and `idle`, plus model fields for package-relative references and validated resolved paths on Agent and Team `ExpertDefinition` values.
- [ ] 1.2 Extend `ExpertDraft` and JSON parsing with `state_avatars`/`stateAvatars` presence semantics, strict known-key/type checks, and normalization that distinguishes omission from an explicit empty object.
- [ ] 1.3 Centralize the supported expert-avatar type and size policy so state validation and HTTP serving share PNG, JPEG, GIF, WebP, BMP, ICO, and the existing 8 MiB limit without changing the main-avatar contract.
- [ ] 1.4 Validate configured state paths as existing regular files contained by the expert package, while preserving unknown future manifest keys during unrelated updates.

## 2. Make package updates lossless and atomic

- [ ] 2.1 Extend manifest loading to read `stateAvatars`, retain safe relative references, and resolve validated files without exposing host paths.
- [ ] 2.2 Extend draft materialization so an omitted object preserves all state data, an explicit object authoritatively updates the three known keys, and an empty object clears known references while retaining copied files and unknown fields.
- [ ] 2.3 Cover create, update, recovery, and invalid-package paths so existing main avatars, GIF bytes, bundled Skills, resources, timestamps, and forward-compatible fields survive atomic transactions.

## 3. Expose safe API data and effective avatar reads

- [ ] 3.1 Add safe `state_avatars` detail data and effective `state_avatar_urls` to expert DTOs without serializing resolved absolute paths or changing `avatar_url`.
- [ ] 3.2 Extend the authenticated avatar endpoint with the optional fixed `state` query, main-avatar fallback, unknown-state rejection, and fallback for post-discovery read races.
- [ ] 3.3 Reuse the existing MIME, size, cache, workspace, containment, and `nosniff` protections for state responses, returning GIF files with `image/gif` and their original bytes.

## 4. Add managed-editor round-trip support

- [ ] 4.1 Extend Web expert normalization, form initialization, payload creation, and validation to carry all three optional package-relative state-avatar paths for Agent and Team forms.
- [ ] 4.2 Add a token-styled optional state-avatar section that labels each state, explains the existing-file requirement, shows configured/effective previews, and supports clearing a reference without implying upload or conversion.
- [ ] 4.3 Keep unsaved state paths and server validation errors visible after a failed save, and preserve existing main-avatar and package-resource behavior when state fields are untouched.
- [ ] 4.4 Synchronize any new visible labels, descriptions, errors, and accessible text through the existing Web i18n catalogs.

## 5. Update Expert Manager and Seed ownership metadata

- [ ] 5.1 Update `expert-manager` JSON and avatar references plus its main workflow to document `stateAvatars`, the three meanings, main-avatar fallback, allowed file types, and original GIF preservation.
- [ ] 5.2 Extend the Python validation/batch paths to accept valid state assets and reject unknown input keys, traversal, missing files, unsupported types, and placeholder references.
- [ ] 5.3 Ensure initialization writes no state paths until real files exist and package/update operations retain original image bytes.
- [ ] 5.4 Refresh `assets/seed/MANIFEST.json`, the Expert Manager source/version metadata, and `seed.version`, preserving the existing ownership-safe rule that user-modified installed Skills are not overwritten.

## 6. Add regression coverage

- [ ] 6.1 Add Registry tests for old-package compatibility, Agent/Team state parsing, strict path/type failures, explicit clear semantics, unknown-key preservation, and lossless package updates.
- [ ] 6.2 Add HTTP tests for safe relative DTOs, effective per-state URLs, configured and fallback reads, unknown state rejection, missing-after-discovery fallback, size/MIME protections, and byte-for-byte animated GIF responses.
- [ ] 6.3 Add Web tests for normalization, Agent/Team form round-trip, payload omission versus explicit clear, previews, and failed-save state preservation.
- [ ] 6.4 Add Expert Manager/Seed tests for valid PNG/GIF packages, invalid state assets, manifest hashes, default seeding, and user-modified ownership protection.

## 7. Verify scope and record deferred runtime work

- [ ] 7.1 Run the focused C++ expert Registry/Web HTTP targets, Expert Manager Python validation fixtures, and focused Web expert-component suites.
- [ ] 7.2 Run the complete relevant C++ unit suite, Web `pnpm test`, `pnpm i18n:audit`, `pnpm build`, and Seed manifest validation gates.
- [ ] 7.3 Verify through an authenticated HTTP/browser check that GIF state assets remain animated-capable, configured states resolve correctly, missing states use the main avatar, and no runtime event automatically changes an avatar.
- [ ] 7.4 After implementation and verification pass, append a new unfinished item to the project `docs/待办.md` describing future subagent-state-driven switching for `working`, `needs_attention`, and `idle`; do not mark it complete.
- [ ] 7.5 Run `openspec validate add-expert-state-avatars --strict` and `git diff --check`, then review the final diff for unintended runtime state-machine, upload, deletion, or unrelated expert behavior.

 