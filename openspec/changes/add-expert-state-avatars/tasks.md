## 1. 添加专家状态头像数据结构

- [x] 1.1 为 `working`、`needs_attention` 和 `idle` 添加共享的固定状态定义，并在 Agent 与 Team 的 `ExpertDefinition` 值中增加包内相对路径引用字段和通过验证的解析路径字段。
- [x] 1.2 扩展 `ExpertDraft` 与 JSON 解析，加入 `state_avatars`/`stateAvatars` 的存在性语义、严格的已知键与类型检查，以及能够区分“省略字段”和“显式空对象”的规范化逻辑。
- [x] 1.3 集中定义受支持的专家头像类型与大小策略，使状态头像校验和 HTTP 提供文件时共用 PNG、JPEG、GIF、WebP、BMP、ICO 以及既有的 8 MiB 限制，同时不改变主头像契约。
- [x] 1.4 校验配置的状态头像路径：目标必须是存在的常规文件并位于专家包内；同时在无关更新中保留未来出现的未知 manifest 字段。

## 2. 确保专家包更新无损且具备原子性

- [x] 2.1 扩展 manifest 加载逻辑以读取 `stateAvatars`、保留安全的相对路径引用，并在不暴露主机路径的前提下解析通过验证的文件。
- [x] 2.2 扩展草稿物化逻辑：省略对象时保留全部状态数据；显式对象权威更新三个已知键；空对象清除已知引用，但保留已复制的文件和未知字段。
- [x] 2.3 覆盖创建、更新、恢复和无效包路径，确保既有主头像、GIF 原始字节、内置 Skills、资源、时间戳和前向兼容字段在原子事务中完整保留。

## 3. 暴露安全的 API 数据与有效头像读取能力

- [x] 3.1 在专家 DTO 中加入安全的 `state_avatars` 详情数据和有效的 `state_avatar_urls`，不得序列化解析后的绝对路径，也不得改变 `avatar_url`。
- [x] 3.2 扩展需要认证的头像端点，支持可选且取值固定的 `state` 查询参数、回退主头像、拒绝未知状态，并在发现文件后发生读取竞态时执行回退。
- [x] 3.3 为状态头像响应复用既有的 MIME、大小、缓存、工作区、路径包含关系和 `nosniff` 防护；返回 GIF 时使用 `image/gif` 并保持原始字节不变。

## 4. 为托管编辑器添加完整往返支持

- [x] 4.1 扩展 Web 端的专家规范化、表单初始化、载荷创建和校验逻辑，使 Agent 与 Team 表单都能携带三种可选的包内相对状态头像路径。
- [x] 4.2 添加采用既有设计令牌样式的可选状态头像区域：标注每种状态、说明文件必须已经存在、展示已配置/实际生效的预览，并支持清除引用，且不暗示系统会上传或转换文件。
- [x] 4.3 保存失败后继续显示尚未保存的状态路径和服务端校验错误；状态字段未被修改时，保留既有主头像与包资源行为。
- [x] 4.4 通过既有 Web i18n 目录同步所有新增的可见标签、说明、错误和可访问性文本。

## 5. 更新 Expert Manager 与 Seed 所有权元数据

- [x] 5.1 更新 `expert-manager` 的 JSON、头像引用及其主工作流，记录 `stateAvatars`、三种状态的含义、主头像回退、允许的文件类型和保留原始 GIF 的要求。
- [x] 5.2 扩展 Python 校验与批处理路径，使其接受有效的状态头像资源，并拒绝未知输入键、路径穿越、文件缺失、不支持的类型和占位引用。
- [x] 5.3 确保初始化流程在真实文件存在之前不会写入状态头像路径，并确保打包/更新操作保留原始图片字节。
- [x] 5.4 刷新 `assets/seed/MANIFEST.json`、Expert Manager 的来源/版本元数据和 `seed.version`，同时维持既有的所有权安全规则：不得覆盖用户修改过的已安装 Skill。

## 6. 添加回归测试覆盖

- [x] 6.1 添加 Registry 测试，覆盖旧包兼容性、Agent/Team 状态解析、严格的路径/类型失败、显式清除语义、未知键保留和无损包更新。
- [x] 6.2 添加 HTTP 测试，覆盖安全相对路径 DTO、各状态的有效 URL、已配置与回退读取、未知状态拒绝、发现后文件缺失的回退、大小/MIME 防护，以及逐字节一致的动态 GIF 响应。
- [x] 6.3 添加 Web 测试，覆盖规范化、Agent/Team 表单往返、载荷省略与显式清除的差异、预览和保存失败后的状态保留。
- [x] 6.4 添加 Expert Manager/Seed 测试，覆盖有效 PNG/GIF 包、无效状态头像资源、manifest 哈希、默认 seed 初始化和用户修改内容的所有权保护。

## 7. 验证范围并记录后续运行时工作

- [x] 7.1 运行针对 C++ 专家 Registry/Web HTTP 的目标测试、Expert Manager Python 校验夹具，以及针对 Web 专家组件的测试套件。
- [x] 7.2 运行完整的相关 C++ 单元测试套件、Web `pnpm test`、`pnpm i18n:audit`、`pnpm build` 和 Seed manifest 校验门禁。
- [x] 7.3 通过经过认证的 HTTP/浏览器检查验证：GIF 状态头像保持动画播放能力；已配置状态解析正确；缺失状态使用主头像；任何运行时事件都不会自动切换头像。
- [x] 7.4 实现与验证全部通过后，在项目唯一待办入口 `docs/ACECode 待办.md` 末尾追加一项未完成任务，描述未来将根据子代理的 `working`、`needs_attention` 和 `idle` 状态动态切换头像；不得将其标记为完成。
- [x] 7.5 运行 `openspec validate add-expert-state-avatars --strict` 和 `git diff --check`，再检查最终差异，确认没有引入计划外的运行时状态机、上传、删除或无关专家行为。
