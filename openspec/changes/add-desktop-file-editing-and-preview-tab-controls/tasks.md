## 1. 安全文本读写基础

- [x] 1.1 为 `MtimeTracker` 增加人工写入后的 Agent 读取基线失效能力并补单元测试
- [x] 1.2 实现 Desktop 专用 `/api/files/editable` 严格读取、版本冲突和保真保存接口
- [x] 1.3 补充工作区边界、非 Desktop 拒绝、编码/换行保真、`HTTP 409` 与写后基线失效的服务端测试
- [x] 1.4 在 Web API 客户端加入可编辑文件读取与保存方法

## 2. Markdown 所见即所得模型

- [x] 2.1 声明 Markdown 序列化直接依赖并实现 GFM MDAST 与 Slate 文档双向转换
- [x] 2.2 覆盖段落、标题、行内样式、链接、引用、代码块、列表、任务列表、表格文字和 opaque 复杂节点往返测试
- [x] 2.3 实现独立 `MarkdownWysiwygEditor`、轻量工具栏、中文输入法保护与键盘保存

## 3. 文件草稿与编辑界面

- [x] 3.1 扩展 `previewTabs.js` 文件标签草稿状态、更新 helpers 与脏标签查询测试
- [x] 3.2 在 `FilePreviewContent` 接入普通文本编辑、Markdown 所见即所得编辑、快捷保存和状态提示
- [x] 3.3 让草稿跨标签/隐藏恢复，并在 Agent 自动刷新或保存冲突时保留草稿
- [x] 3.4 为单个、其他、右侧和全部标签关闭接入脏草稿确认，大点状态与关闭行为保持可访问

## 4. 详情标签工作台交互

- [x] 4.1 在标签旁实现“+”弹出菜单及终端、浏览器、侧边聊天三个动作
- [x] 4.2 使用现有 side-question 能力实现不影响主草稿的内部侧边聊天输入卡
- [x] 4.3 将详情叉号改为只隐藏，并确保重新打开恢复标签、草稿和浏览器页面
- [x] 4.4 完成菜单、大点、编辑器和隐藏状态的样式及 WebView 兼容回退

## 5. 文档与验证

- [x] 5.1 更新 daemon 文件 API 与 Desktop 文件详情行为文档
- [x] 5.2 运行 Web 单测与构建、相关 C++ 单测、严格 OpenSpec 校验和 `git diff --check`
- [x] 5.3 在 Desktop/WebView 中手动验证参考图对应的大点、加号菜单、隐藏恢复、文本保存与 Markdown 所见即所得流程

## 6. 验收修正：默认编辑与关闭保存

- [x] 6.1 更新 OpenSpec，统一为打开即编辑、普通文本保持显色、无编辑/保存/取消按钮及关闭三选一
- [x] 6.2 实现普通文本默认显色编辑与 Markdown 默认所见即所得编辑，并保留 `Ctrl+S` 保存
- [x] 6.3 实现脏页签关闭时“保存并关闭 / 不保存 / 取消”，覆盖单个与批量关闭及保存失败保护
- [x] 6.4 更新前端测试和文档，并完成 Web 测试、构建、严格 OpenSpec 校验与差异检查（全量测试仅保留既有 Provider 图标数量 185/193 基线失败）
