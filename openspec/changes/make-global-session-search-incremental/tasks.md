## 1. 增量目录基础

- [x] 1.1 为 `SessionStorage::list_session_metadata()` 和单项目目录扫描增加可选取消检查，并用单元测试证明大型项目能在文件边界停止。
- [x] 1.2 在 session 层实现按项目分片的 `GlobalSessionCatalogIndex`，包含后台预热、进度/generation、暂停续扫、活跃会话合并和局部错误。
- [x] 1.3 实现项目分片精确失效与低频目录变化发现，确保刷新单项目不会清空其它已完成分片。
- [x] 1.4 添加增量索引单元测试，覆盖部分快照、取消后续扫、隐藏/无工作区会话、归档/子代理过滤和单项目刷新。

## 2. 有界搜索与取消 API

- [x] 2.1 实现服务端元数据评分、limit、generation cursor 和精简搜索 DTO；空查询最多返回最近 50 条。
- [x] 2.2 实现按 `request_id + query` 管理的正文搜索短批次作业、进度累计、闲置清理和取消令牌。
- [x] 2.3 新增幂等取消端点，并将会话元数据与正文搜索路由切换到增量索引/作业，保留旧顶层数组兼容字段。
- [x] 2.4 将 WebServer 生命周期接入索引启动/停止，并在归档、恢复、删除、重命名及 workspace 变化后精确失效对应项目。
- [x] 2.5 更新 `docs/daemon-api.md`，记录查询参数、精简字段、进度、分页、cursor 失效和取消契约。
- [x] 2.6 添加 C++ 单元与 Web smoke tests，覆盖有界响应、无完整字段泄漏、稳定分页、部分进度、重复取消及取消后停止推进。

## 3. Web 渐进搜索体验

- [x] 3.1 扩展 `api.js` 支持外部 `AbortSignal`、增量元数据/正文参数、取消端点和 cursor 失效分类。
- [x] 3.2 重构 `SearchPalette`：立即显示部分结果与索引进度，短批轮询正文命中，完整后支持加载更多，不再等待全局 loading。
- [x] 3.3 统一关闭按钮、Escape、遮罩、查询替换和卸载的取消路径；取消保持安静，真实错误显示重试且不覆盖最近成功结果。
- [x] 3.4 更新搜索纯逻辑、API、组件架构和交互测试，证明每个关闭入口都会本地 abort 并通知服务端取消。

## 4. 验证与性能回归

- [x] 4.1 运行相关 C++ 单元/Smoke tests、Web 全量测试和 Web 构建，修复全部回归。
- [x] 4.2 运行 OpenSpec strict validation、代码质量检查与 `git diff --check`，确认未覆盖用户无关改动。
- [x] 4.3 使用本机大规模 `~/.acecode/projects` 实测首次打开、增量进度、响应体积、重复打开和关闭取消，记录结果并确认不再触发 30 秒超时。
