## 1. 聊天区拖放边界

- [x] 1.1 让 `InputBar` 暴露并复用现有文件拖放处理方法，同时统一同步本地与聊天区激活状态
- [x] 1.2 在新会话空态和已有会话主栏绑定拖放委托，并渲染全栏释放提示

## 2. 视觉与附件间距

- [x] 2.1 实现主题化的半透明降对比、渐进式 backdrop blur、中央提示与 reduced-motion 处理
- [x] 2.2 为 Slate composer 内联 tag 增加 5px 水平 gutter 和轻微垂直间距，保持消息徽标不变

## 3. 回归验证

- [x] 3.1 增加前端架构测试，覆盖聊天区命中范围、处理链路复用、反馈生命周期和 scoped tag gutter
- [x] 3.2 运行 `pnpm test`、`pnpm build` 与 OpenSpec 严格校验，并审计最终差异未改动后端附件合同

## 4. Desktop 拖入前置

- [x] 4.1 增加文件拖入专用 Desktop 激活桥接，并只在拖拽深度首次从 0 进入时调用一次
- [x] 4.2 将文件加入后的 composer 光标恢复与原生窗口前置拆开，防止 drop 后持续抢焦点

## 5. 同一文件去重

- [x] 5.1 增加来源路径优先、浏览器文件元数据回退的附件身份函数及单元测试
- [x] 5.2 在会话创建和上传前预留身份，覆盖批内重复、composer 已有附件、移除与失败释放

## 6. 增量回归验证

- [x] 6.1 扩展架构测试，锁定一次性拖入激活、drop 后无原生抢焦点与 ChatView 去重接线
- [x] 6.2 运行 `pnpm test`、`pnpm build`、相关 Desktop 构建/测试与 OpenSpec 严格校验，并审计最终差异
