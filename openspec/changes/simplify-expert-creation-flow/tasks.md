## 1. 固化对话式创建契约

- [x] 1.1 扩展专家组件架构测试，要求提供专用的对话式创建回调、导航至同一工作区的主页，并在不绑定专家的情况下准确预置 `/expert-manager ` 草稿。
- [x] 1.2 增加分段控件契约覆盖，证明主按钮段和箭头段使用独立的处理器、菜单仅包含 `高级模式`，且任何处理器都不会自动创建或发送消息。
- [x] 1.3 增加菜单可访问性契约覆盖，验证 `menu`/`menuitem` 语义、展开状态、按 Escape 后恢复焦点、点击外部关闭，以及原生界面重叠标记。

## 2. 将主操作路由到正式会话输入框

- [x] 2.1 在 Web 导航层定义 Expert Manager 预置草稿常量，并保留其必需的尾随空格。
- [x] 2.2 在 `App` 中新增对话式创建处理器：解析当前专家页面所属的工作区、刷新其 Git 信息，并仅携带 `initialDraftText` 导航到真实的主页输入框。
- [x] 2.3 将新处理器接入 `ExpertComponentsPage`，并验证现有的一次性草稿消费逻辑只更新目标工作区的草稿，且不会在用户编辑后再次应用。

## 3. 实现分段式新建专家控件

- [x] 3.1 将单一的 `新建专家` 按钮替换为采用既有设计令牌样式的 32px 分段控件；保留既有主按钮的图标、标签、内边距优先关系与点击区域，同时增加独立的箭头段。
- [x] 3.2 实现局部菜单状态、锚点/焦点引用、Escape 与外部指针事件清理，以及组件卸载时关闭菜单的行为，且不影响 `组建专家团`。
- [x] 3.3 渲染唯一的 `高级模式` 菜单项；选择后先关闭菜单，再使用 `emptyExpertForm('agent')` 打开既有的 Agent `ExpertEditor`。

## 4. 同步本地化 UI 文案

- [x] 4.1 使用现有 i18n 生成流程，将 `高级模式` 及所有新增的可访问性标签/工具提示加入源语言目录和英文覆盖目录。
- [x] 4.2 确认分段控件和弹出菜单在浅色与深色主题下仅使用 ACECode 既有的字体、颜色、边框、焦点和浮层设计令牌。

## 5. 验证完整变更

- [x] 5.1 运行针对专家组件、主页草稿、工作区导航和 i18n 的架构测试，并修复所有契约回归。
- [x] 5.2 运行完整的 Web `pnpm test`、`pnpm i18n:audit` 和 `pnpm build` 验证门禁。
- [x] 5.3 在桌面端与窄屏布局中手动验证：点击主按钮会聚焦并预置未发送的 Skill；点击箭头仍停留在当前页面；选择高级模式会打开原有编辑器；按 Escape 或点击外部会关闭菜单。
- [x] 5.4 运行 `openspec validate simplify-expert-creation-flow --strict` 和 `git diff --check`，再检查最终差异，确认没有意外扩大到产品、API 或通用对话入口范围。
